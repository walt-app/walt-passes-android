# No consumer ProGuard rules: passes-document is pure orchestration over the public APIs of
# passes-pdf / passes-image / passes-pdf-core. It ships no reflection, no JNI, and no service
# of its own, so it imposes nothing on the consumer's shrinker config.
