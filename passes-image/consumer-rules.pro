# Consumer ProGuard rules contributed by passes-image.
# Empty for now: the decode facade and binder are plain Kotlin types and the isolated decode
# service is referenced by manifest entries AGP keeps automatically, so R8 has no special-case
# stripping risk to head off here yet.
